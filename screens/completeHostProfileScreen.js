import React from "react";
import { View, Text, ScrollView } from "react-native";

const completeHostProfileScreen = () => {
  return (
    <ScrollView>
      <Text>Complete Porfile</Text>
    </ScrollView>
  );
};

export default completeHostProfileScreen;
